# Lightweight AI Trainer

This repository contains the deployable source bundle and Render configuration
for Lightweight AI Trainer. `trainer.zip` is the full application source.

Render builds the Docker image directly from this repository. Set a
`TRAINER_API_KEY` before using a public deployment.
